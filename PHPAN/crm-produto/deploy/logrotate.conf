# /etc/logrotate.d/crm-produto
# Sem rotação, um dia o disco enche — e o incidente vira "servidor travado por
# log", pior que o bug original.
/var/www/crm-produto/shared/var/logs/*.jsonl {
    daily
    rotate 14
    compress
    missingok
    notifempty
    copytruncate
}
